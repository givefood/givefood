# Keeps the Docker build context small. Without this the 500MB+ .git
# directory is sent to the daemon on every build.
.git
.github
.venv
__pycache__/
*.py[cod]
*.so
.pytest_cache/
*.egg-info/
.env
.DS_Store
test-report.html
test-results.xml
coverage.xml
.coverage
htmlcov/
